<!DOCTYPE html>
<html lang="hu">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="description" content="Gallery website for tattoo artist Dalma Bruder" />
  <meta name="keywords" content="tattoo,artist,dalma,bruder,dalmabruder,dalmabrudertattoo,pecs,pécs,Pécs,tetovalas,tetkó,tat,art,ink,tattoolife" />

  <title>Dalma Bruder Tattoo</title>

  <link rel="stylesheet" type="text/css" href="{{relative 'public/core.css'}}" />
  <link rel="stylesheet" type="text/css" href="{{relative 'public/theme.css'}}" />
  <link rel="stylesheet" type="text/css" href="{{relative 'public/css/lightgallery.min.css'}}" />
</head>
<body>
  <div id="main">
    <header>
      DALMABRUDERTATTOO
    </header>

    <sidebar>
      {{#each (objectKeys (groupByYearAndMonth album.files))}}
        <div class="month" id="{{slugify this}}">{{this}}</div>
      {{/each}}
    </sidebar>

    <content id="albums">
      {{#each (groupByYearAndMonth album.files)}}
        <div id="{{slugify @key}}">
          {{@key}}
          <hr />
          {{#each this}}
            <a class="item" data-src="{{relative urls.large}}">
              <img src="{{relative urls.small}}">
            </a>
          {{/each}}
        </div>
      {{/each}}
    </content>
  </div>

  <script src="{{relative 'public/js/jquery.min.js'}}"></script>
  <script src="{{relative 'public/js/lightgallery.min.js'}}"></script>
  <script src="{{relative 'public/js/jquery.mousewheel.min.js'}}"></script>
  <script src="{{relative 'public/js/theme.js'}}" type="text/javascript"></script>
</body>
</html>

